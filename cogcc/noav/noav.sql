create table cogcc_noavs (
	well_id integer, 
	document_number integer, 
	violation_date varchar(20),  
	alleged_violation varchar(5000), 
	cited_condition varchar(2000), 
	abatement varchar(5000), 
	abatement_date varchar(20)
);